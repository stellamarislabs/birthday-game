// Normal player-facing platformer screens stay clean; the F1 dev editor owns verbose object labels.
export const PLAYER_WORLD_LABELS_VISIBLE = false;

export const PLATFORMER_PLAYER_HUD_POLICY = {
  showPersistentControlHint: false,
  showPersistentSoundStatus: false
} as const;
